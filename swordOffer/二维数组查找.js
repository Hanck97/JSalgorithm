/*
 * @Author: hanyang
 * @Description: 二维数组查找
 * @Date: 2019-08-23 15:12:44
 */


/**
 * @description: 在一个二维数组中（每个一维数组的长度相同），
 * 每一行都按照从左到右递增的顺序排序，每一列都按照从上到下递增的顺序排序。
 * 请完成一个函数，输入这样的一个二维数组和一个整数，判断数组中是否含有该整数。
 * @param {type} array number 
 * @return: boolean
 */

function FindNumber(target, array) {
    
}